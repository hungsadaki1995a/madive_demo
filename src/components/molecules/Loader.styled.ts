import { styled } from '@mui/material';

const Wrapper = styled('div')(() => ({
  width: '100%',
  minHeight: '50vh',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
}));

export { Wrapper };
